import NavBar from './NavBar';

const Header = () => {
    return (
        <header>
            <a href="/" className="logo">LogoName</a>
        <div>
            <NavBar />
        </div>
        </header>
    );
}

export default Header;  
